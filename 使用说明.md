# 交易复盘系统 - 操作型看板使用说明

## 📌 概述

这是一个**交互式操作型看板**（不是静态报告），数据存储在飞书多维表格中，网页自动从飞书API读取数据。数据变更后，刷新网页即可看到最新图表，无需重新生成。

## 🚀 快速开始

### 1. 部署到GitHub Pages

```bash
# 1. 在GitHub创建仓库（如 trading-dashboard）
# 2. 将以下文件推送到仓库：
#    - index.html
#    - style.css
#    - app.js
#    - feishu-api.js
#    - compliance-engine.js
#    - config.js

# 3. 在仓库Settings → Pages中启用GitHub Pages
# 4. 访问 https://你的用户名.github.io/trading-dashboard/
```

### 2. 配置飞书API连接

编辑 `config.js` 文件：

```javascript
const CONFIG = {
    APP_ID: 'cli_xxxxxxxxxxxxxx',      // 飞书开放平台 → 应用管理 → App ID
    APP_SECRET: 'xxxxxxxxxxxxxxxx',     // 飞书开放平台 → 应用管理 → App Secret
    BASE_TOKEN: 'SdUMbTbtzaHbmTsSapvc1BnInRb',  // 已填好
    // ...
};
```

> ⚠️ 安全提示：config.js中包含App Secret，**不要将此文件提交到公开仓库**。
> 建议：将config.js加入.gitignore，或使用GitHub私有仓库。

### 3. 飞书应用权限配置

在飞书开放平台，确保应用已开通以下权限：
- `bitable:app` - 查看、评论、编辑和管理多维表格
- 应用已添加为多维表格的协作者（至少可编辑权限）

## 📊 功能模块

### 市场总览
- **6大宏观指标卡片**：VIX、ERP、DXY、PE百分位、市场温度、宏观评分
- **4个趋势图表**：VIX&ERP趋势、估值百分位走势、DXY&融资数据、宏观评分历史
- 数据来源：飞书「宏观数据_自动」「宏观数据_手动」表

### 持仓管理
- 查看当前持仓明细、盈亏情况
- 添加/编辑持仓
- 持仓分布饼图、盈亏柱状图
- 数据来源：飞书「持仓数据」表

### 合规检查
- **输入拟执行的操作**（买入/卖出/加仓/再平衡）
- **自动检查v1.4规则合规性**
- 显示通过/未通过/警告的检查项
- 规则参数可自定义（见参数配置）

### 交易记录
- 记录每笔交易操作
- 自动保存到飞书「交易记录」表
- 数据来源：飞书「交易记录」表

### 参数配置
- **可视化修改v1.4的所有规则参数**
- 修改后保存到浏览器localStorage
- 点击"保存参数"同步到飞书「规则参数配置」表
- "恢复默认"还原v1.4原始参数

## 🗄️ 飞书多维表格结构

| 表名 | 用途 | 数据更新方式 |
|------|------|------------|
| 宏观数据_自动 | VIX、美股ERP等 | Agent自动写入 |
| 宏观数据_手动 | A股ERP、DXY、融资数据 | 用户从iFind导出 |
| 持仓数据 | 当前持仓明细 | 用户手动填写 |
| 交易记录 | 买卖操作记录 | 网页/手动填写 |
| 指数估值数据 | PE/PB百分位 | Agent/手动更新 |
| 股票历史数据 | 个股K线 | Agent自动写入 |
| 规则参数配置 | 可调整的交易规则参数 | 网页同步 |
| 融资买入额占比 | 原"市场广度" | Agent写入 |
| 融资净买入 | 原"ETF资金流向" | Agent写入 |

## 🔧 技术架构

```
飞书多维表格（数据层）
    ↕ Feishu Open API（RESTful）
GitHub Pages 静态网页（展示层）
    ├── config.js（配置）
    ├── feishu-api.js（API集成）
    ├── compliance-engine.js（合规引擎）
    ├── app.js（主逻辑+ECharts）
    └── style.css（深色科技风UI）
```

- **无后端服务器**：纯静态HTML+CSS+JS
- **数据驱动**：飞书数据变 → 图表自动更新
- **跨域支持**：飞书API原生支持CORS，浏览器直连
- **离线模式**：未配置飞书凭证时，使用localStorage缓存数据

## 📝 日常使用流程

1. **每周五**：打开看板 → 检查宏观指标 → 执行合规检查 → 执行定投操作
2. **操作后**：在"交易记录"页记录操作
3. **定期**：在飞书更新持仓数据、估值数据
4. **需要时**：调整参数配置，观察不同参数下的合规检查结果

---

*基于交易规则v1.4，最后更新：2026-08-06*
